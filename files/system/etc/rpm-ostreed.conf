# kb3lyb-sway (DESIGN §5). Stage upgrades in the background so they apply on the
# next reboot — no surprise auto-reboots. Paired with rpm-ostreed-automatic.timer
# (enabled in the recipe). Rebases to a new Fedora are still manual (§5/§8.1).
[Daemon]
AutomaticUpdatePolicy=stage
